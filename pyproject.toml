[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mcp-biomodelling-servers"
version = "2.3.0"
description = "MCP servers for NeKo signalling-network construction, MaBoSS Boolean simulation, and PhysiCell configuration"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
keywords = ["mcp", "systems-biology", "bioinformatics", "neko", "maboss", "physicell"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "License :: OSI Approved :: MIT License",
]
dependencies = [
    "mcp>=2,<3",
    "anyio>=4.9; python_version < '3.14'",
    "anyio>=4.10; python_version >= '3.14'",
    "pydantic>=2.12",
    "pandas>=2.2,<4",
    "matplotlib>=3.8,<4",
    "nekomata>=1.10.0,<2",
    "maboss>=0.8.15,<0.9",
    "physicell-settings>=0.6.2,<0.7",
    "requests>=2.31,<3",
    "tabulate>=0.9,<1",
]

[project.optional-dependencies]
dev = [
    "build>=1.2",
    "mypy>=1.10,<2",
    "pytest>=8",
    "ruff==0.16.0",
]

[project.urls]
Homepage = "https://github.com/saran-pankaew/mcp-biomodelling-servers"
Repository = "https://github.com/saran-pankaew/mcp-biomodelling-servers"

[project.scripts]
mcp-neko-server = "mcp_biomodelling_servers.neko:main"
mcp-maboss-server = "mcp_biomodelling_servers.maboss:main"
mcp-physicell-server = "mcp_biomodelling_servers.physicell:main"

[tool.ruff]
# Keep emitted syntax compatible with the project's minimum supported Python.
# This does not impose an upper bound; Python 3.14 remains supported.
target-version = "py310"

[tool.ruff.lint]
# Pin the lint contract so upgrades to Ruff cannot silently change CI policy.
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "BLE", "C4"]

[tool.pytest.ini_options]
# Any use of an SDK API deprecated by MCP should fail the compatibility suite.
filterwarnings = [
    "error::mcp.shared.exceptions.MCPDeprecationWarning",
]

# ---------------------------------------------------------------------------
# Build configuration
# Each server directory is mapped into the mcp_biomodelling_servers namespace
# package so that server.py's `Path(__file__).parent.parent` resolves to the
# mcp_biomodelling_servers/ directory, where artifact_manager.py lives.
# ---------------------------------------------------------------------------
[tool.hatch.build.targets.wheel]
packages = [
    "mcp_biomodelling_servers",
    "NeKo",
    "MaBoSS",
    "PhysiCell",
]
exclude = [
    "**/artifacts/**",
    "**/exports/**",
    "**/pypath_log/**",
    "tests/**",
    "**/*.bnet",
    "**/*.sif",
    "**/server.json",
    "**/.env",
    "**/.env.*",
]

[tool.hatch.build.targets.wheel.sources]
# Remap server directories into the package namespace so that installed paths
# look like:  mcp_biomodelling_servers/NeKo/server.py  (parent.parent = mcp_biomodelling_servers/)
"NeKo"      = "mcp_biomodelling_servers/NeKo"
"MaBoSS"    = "mcp_biomodelling_servers/MaBoSS"
"PhysiCell" = "mcp_biomodelling_servers/PhysiCell"

[tool.hatch.build.targets.sdist]
include = [
    "mcp_biomodelling_servers/",
    "NeKo/",
    "MaBoSS/",
    "PhysiCell/",
    "artifact_manager.py",
    "README.md",
    "pyproject.toml",
]
exclude = [
    "**/artifacts/**",
    "**/exports/**",
    "**/pypath_log/**",
    "tests/**",
    "**/.env",
    "**/.env.*",
]
